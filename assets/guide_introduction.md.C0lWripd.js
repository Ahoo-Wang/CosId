import{_ as t}from"./chunks/sharding-db.CcJS1N71.js";import{_ as o,a as d,b as n}from"./chunks/Machine-Id-Safe-Guard.BSblLDvL.js";import{_ as l}from"./chunks/SegmentId.DTId14gI.js";import{_ as i}from"./chunks/SegmentChainId.C8u2j_Ci.js";import{_ as a,c as r,a2 as s,o as c}from"./chunks/framework.Dg3afQf7.js";const g="/assets/monotonically-increasing.BbURzAwS.png",I="/assets/trend-increasing.DL8SdQIH.png",w=JSON.parse('{"title":"简介","description":"","frontmatter":{},"headers":[],"relativePath":"guide/introduction.md","filePath":"guide/introduction.md","lastUpdated":1760420434000}'),h={name:"guide/introduction.md"};function m(u,e,p,S,b,f){return c(),r("div",null,e[0]||(e[0]=[s('<h1 id="简介" tabindex="-1">简介 <a class="header-anchor" href="#简介" aria-label="Permalink to &quot;简介&quot;">​</a></h1><p><em><a href="https://github.com/Ahoo-Wang/CosId" target="_blank" rel="noreferrer">CosId</a></em> 旨在提供通用、灵活、高性能的分布式 ID 生成器。</p><ul><li><code>CosIdGenerator</code> : <em>单机 TPS 性能：1557W/s</em>，三倍于 <code>UUID.randomUUID()</code>，基于时钟的全局趋势递增ID，可以同时支持一百万个实例。</li><li><code>SnowflakeId</code> : <em>单机 TPS 性能：409W/s</em> <a href="./faq/perf-test.html">JMH 基准测试</a> , 主要解决 <em>时钟回拨</em> 、<em>机器号分配</em>、<em>取模分片不均匀</em> 等问题并提供更加友好、灵活的使用体验。</li><li><code>SegmentId</code>: 每次获取一段 (<code>Step</code>) ID，来降低号段分发器的网络IO请求频次提升性能。并提供多种号段分发器实现。</li><li><code>SegmentChainId</code>:<code>SegmentChainId</code> (<em>lock-free</em>) 是对 <code>SegmentId</code> 的增强。性能可达到近似 <code>AtomicLong</code> 的 <em>TPS 性能:12743W+/s</em> <a href="./faq/perf-test.html">JMH 基准测试</a> 。 <ul><li><code>PrefetchWorker</code> 维护安全距离(<code>safeDistance</code>), 并且支持基于饥饿状态的动态<code>safeDistance</code>扩容/收缩。</li><li>适应性：相比于 <code>SegmentId</code>，<code>SegmentChainId</code> 可以根据业务场景动态调整 <code>Step</code> 来提升性能。</li></ul></li></ul><h2 id="背景-为什么需要分布式id" tabindex="-1">背景（为什么需要<em>分布式ID</em>） <a class="header-anchor" href="#背景-为什么需要分布式id" aria-label="Permalink to &quot;背景（为什么需要*分布式ID*）&quot;">​</a></h2><p>在软件系统演进过程中，随着业务规模的增长 (TPS/存储容量)，我们需要通过集群化部署来分摊计算、存储压力。 应用服务的无状态设计使其具备了伸缩性。在使用 <strong>Kubernetes</strong> 部署时我们只需要一行命令即可完成服务伸缩 (<code>kubectl scale --replicas=5 deployment/order-service</code>)。</p><p>但对于有状态的数据库就不那么容易了，此时数据库变成系统的性能瓶颈是显而易见的。</p><h3 id="分库分表" tabindex="-1">分库分表 <a class="header-anchor" href="#分库分表" aria-label="Permalink to &quot;分库分表&quot;">​</a></h3><div class="info custom-block"><p class="custom-block-title">INFO</p><p>从微服务的角度来理解垂直拆分其实就是微服务拆分。以限界上下文来定义服务边界将大服务/单体应用拆分成多个自治的粒度更小的服务，因为自治性规范要求，数据库也需要进行业务拆分。</p><p>但垂直拆分后的单个微服务依然会面临 TPS/存储容量 的挑战，所以这里我们重点讨论水平拆分的方式。</p></div><p align="center"><img src="'+t+'" alt="分库分表"></p><p>数据库分库分表方案是逻辑统一，物理分区自治的方案。其核心设计在于中间层映射方案的设计 (上图 <strong>Mapping</strong>)，即分片算法的设计。 几乎所有编程语言都内置实现了散列表(java:<code>HashMap</code>/csharp:<code>Dictionary</code>/python:<code>dict</code>/go:<code>map</code> ...)。分片算法跟散列表高度相似(<code>hashCode</code>)，都得通过 <code>key</code>/<code>shardingValue</code> 映射到对应的槽位(<code>slot</code>)。</p><p>那么 <code>shardingValue</code> 从哪里来呢？<strong>CosId</strong>！！！</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>当然还有很多分布式场景需要<em>分布式ID</em>，这里不再一一列举。</p></div><h2 id="分布式id方案的核心指标" tabindex="-1">分布式ID方案的核心指标 <a class="header-anchor" href="#分布式id方案的核心指标" aria-label="Permalink to &quot;分布式ID方案的核心指标&quot;">​</a></h2><ul><li><strong>全局（相同业务）唯一性</strong>：唯一性保证是<strong>ID</strong>的必要条件，假设ID不唯一就会产生主键冲突，这点很容易可以理解。 <ul><li>通常所说的全局唯一性并不是指所有业务服务都要唯一，而是相同业务服务不同部署副本唯一。 比如 Order 服务的多个部署副本在生成<code>t_order</code>这张表的<code>Id</code>时是要求全局唯一的。至于<code>t_order_item</code>生成的<code>ID</code>与<code>t_order</code>是否唯一，并不影响唯一性约束，也不会产生什么副作用。 不同业务模块间也是同理。即唯一性主要解决的是ID冲突问题。</li></ul></li><li><strong>有序性</strong>：有序性保证是面向查询的数据结构算法（除了Hash算法）所必须的，是<strong>二分查找法</strong>(分而治之)的前提。 <ul><li>MySq-InnoDB B+树是使用最为广泛的，假设 Id 是无序的，B+ 树 为了维护 ID 的有序性，就会频繁的在索引的中间位置插入而挪动后面节点的位置，甚至导致频繁的页分裂，这对于性能的影响是极大的。那么如果我们能够保证ID的有序性这种情况就完全不同了，只需要进行追加写操作。所以 ID 的有序性是非常重要的，也是ID设计不可避免的特性。</li></ul></li><li><strong>吞吐量/性能(ops/time)</strong>：即单位时间（每秒）能产生的ID数量。生成ID是非常高频的操作，也是最为基本的。假设ID生成的性能缓慢，那么不管怎么进行系统优化也无法获得更好的性能。 <ul><li>一般我们会首先生成ID，然后再执行写入操作，假设ID生成缓慢，那么整体性能上限就会受到限制，这一点应该不难理解。</li></ul></li><li><strong>稳定性(time/op)</strong>：稳定性指标一般可以采用<strong>每个操作的时间进行百分位采样</strong>来分析，比如 <em><a href="https://github.com/Ahoo-Wang/CosId" target="_blank" rel="noreferrer">CosId</a></em> 百分位采样 <strong>P9999=0.208 us/op</strong>，即 <strong>0% ~ 99.99%</strong> 的单位操作时间小于等于 <strong>0.208 us/op</strong>。 <ul><li><a href="https://zh.wikipedia.org/wiki/%E7%99%BE%E5%88%86%E4%BD%8D%E6%95%B0" target="_blank" rel="noreferrer">百分位数 WIKI</a> ：统计学术语，若将一组数据从小到大排序，并计算相应的累计百分点，则某百分点所对应数据的值，就称为这百分点的百分位数，以Pk表示第k百分位数。百分位数是用来比较个体在群体中的相对地位量数。</li><li>为什么不用平均<em>每个操作的时间</em>：马老师的身价跟你的身价能平均么？平均后的值有意义不？</li><li>可以使用最小<em>每个操作的时间</em>、最大<em>每个操作的时间</em>作为参考吗？因为最小、最大值只说明了零界点的情况，虽说可以作为稳定性的参考，但依然不够全面。而且<em>百分位数</em>已经覆盖了这俩个指标。</li></ul></li><li><strong>自治性（依赖）</strong>：主要是指对外部环境有无依赖，比如<strong>号段模式</strong>会强依赖第三方存储中间件来获取<code>NexMaxId</code>。自治性还会对可用性造成影响。</li><li><strong>可用性</strong>：分布式ID的可用性主要会受到自治性影响，比如<strong>SnowflakeId</strong>会受到时钟回拨影响，导致处于短暂时间的不可用状态。而<strong>号段模式</strong>会受到第三方发号器（<code>NexMaxId</code>）的可用性影响。 <ul><li><a href="https://zh.wikipedia.org/wiki/%E5%8F%AF%E7%94%A8%E6%80%A7" target="_blank" rel="noreferrer">可用性 WIKI</a> ：在一个给定的时间间隔内，对于一个功能个体来讲，总的可用时间所占的比例。</li><li><em>MTBF</em>：平均故障间隔</li><li><em>MDT</em>：平均修复/恢复时间</li><li><code>Availability=MTBF/(MTBF+MDT)</code></li><li>假设<em>MTBF</em>为1年，<em>MDT</em>为1小时，即<code>Availability=(365*24)/(365*24+1)=0.999885857778792≈99.99%</code>，也就是我们通常所说对可用性4个9。</li></ul></li><li><strong>适应性</strong>：是指在面对外部环境变化的自适应能力，这里我们主要说的是面对流量突发时动态伸缩分布式ID的性能， <ul><li><strong>SegmentChainId</strong>可以基于<strong>饥饿状态</strong>进行<strong>安全距离</strong>的动态伸缩。</li><li><strong>SnowflakeId</strong>常规位分配方案性能恒定409.6W，虽然可以通过调整位分配方案来获得不同的TPS性能，但是位分配方法的变更是破坏性的，一般根据业务场景确定位分配方案后不再变更。</li></ul></li><li><strong>存储空间</strong>：还是用MySq-InnoDB B+树来举例，普通索引（二级索引）会存储主键值，主键越大占用的内存缓存、磁盘空间也会越大。Page页存储的数据越少，磁盘IO访问的次数会增加。总之在满足业务需求的情况下，尽可能小的存储空间占用在绝大多数场景下都是好的设计原则。</li></ul><h3 id="不同分布式id方案核心指标对比" tabindex="-1">不同分布式ID方案核心指标对比 <a class="header-anchor" href="#不同分布式id方案核心指标对比" aria-label="Permalink to &quot;不同分布式ID方案核心指标对比&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:center;">分布式ID</th><th style="text-align:center;">全局唯一性</th><th style="text-align:center;">有序性</th><th style="text-align:right;">吞吐量</th><th style="text-align:left;">稳定性（1s=1000,000us）</th><th style="text-align:center;">自治性</th><th style="text-align:center;">可用性</th><th style="text-align:center;">适应性</th><th style="text-align:center;">存储空间</th></tr></thead><tbody><tr><td style="text-align:center;">UUID/GUID</td><td style="text-align:center;">是</td><td style="text-align:center;">完全无序</td><td style="text-align:right;">3078638(ops/s)</td><td style="text-align:left;">P9999=0.325(us/op)</td><td style="text-align:center;">完全自治</td><td style="text-align:center;">100%</td><td style="text-align:center;">否</td><td style="text-align:center;">128-bit</td></tr><tr><td style="text-align:center;">SnowflakeId</td><td style="text-align:center;">是</td><td style="text-align:center;">本地单调递增，全局趋势递增(受全局时钟影响)</td><td style="text-align:right;">4096000(ops/s)</td><td style="text-align:left;">P9999=0.244(us/op)</td><td style="text-align:center;">依赖时钟</td><td style="text-align:center;">时钟回拨会导致短暂不可用</td><td style="text-align:center;">否</td><td style="text-align:center;">64-bit</td></tr><tr><td style="text-align:center;">SegmentId</td><td style="text-align:center;">是</td><td style="text-align:center;">本地单调递增，全局趋势递增(受Step影响)</td><td style="text-align:right;">29506073(ops/s)</td><td style="text-align:left;">P9999=46.624(us/op)</td><td style="text-align:center;">依赖第三方号段分发器</td><td style="text-align:center;">受号段分发器可用性影响</td><td style="text-align:center;">否</td><td style="text-align:center;">64-bit</td></tr><tr><td style="text-align:center;">SegmentChainId</td><td style="text-align:center;">是</td><td style="text-align:center;">本地单调递增，全局趋势递增(受Step、安全距离影响)</td><td style="text-align:right;">127439148(ops/s)</td><td style="text-align:left;">P9999=0.208(us/op)</td><td style="text-align:center;">依赖第三方号段分发器</td><td style="text-align:center;">受号段分发器可用性影响，但因安全距离存在，预留ID段，所以高于SegmentId</td><td style="text-align:center;">是</td><td style="text-align:center;">64-bit</td></tr></tbody></table><h3 id="有序性-要想分而治之·二分查找法-必须要维护我" tabindex="-1">有序性(要想分而治之·二分查找法，必须要维护我) <a class="header-anchor" href="#有序性-要想分而治之·二分查找法-必须要维护我" aria-label="Permalink to &quot;有序性(要想分而治之·二分查找法，必须要维护我)&quot;">​</a></h3><p>刚刚我们已经讨论了ID有序性的重要性，所以我们设计ID算法时应该尽可能地让ID是单调递增的，比如像表的自增主键那样。但是很遗憾，因全局时钟、性能等分布式系统问题，我们通常只能选择局部单调递增、全局趋势递增的组合（就像我们在分布式系统中不得不的选择最终一致性那样）以获得多方面的权衡。下面我们来看一下什么是单调递增与趋势递增。</p><h4 id="有序性之单调递增" tabindex="-1">有序性之单调递增 <a class="header-anchor" href="#有序性之单调递增" aria-label="Permalink to &quot;有序性之单调递增&quot;">​</a></h4><p align="center"><img src="'+g+'" alt="单调递增"></p><p>单调递增：T表示全局绝对时点，假设有T<sub>n+1</sub>&gt;T<sub>n</sub>（绝对时间总是往前进的，这里不考虑相对论、时间机器等），那么必然有F(T<sub>n+1</sub>)&gt;F(T<sub>n</sub>)，数据库自增主键就属于这一类。 另外需要特别说明的是单调递增跟连续性递增是不同的概念。 连续性递增：<code>F(n+1)=(F(n)+step)</code>即下一次获取的ID一定等于当前<code>ID+Step</code>，当<code>Step=1</code>时类似于这样一个序列:<code>1-&gt;2-&gt;3-&gt;4-&gt;5</code>。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>扩展小知识：数据库的自增主键也不是连续性递增的，相信你一定遇到过这种情况，请思考一下数据库为什么这样设计？</p></div><h4 id="有序性之趋势递增" tabindex="-1">有序性之趋势递增 <a class="header-anchor" href="#有序性之趋势递增" aria-label="Permalink to &quot;有序性之趋势递增&quot;">​</a></h4><p align="center"><img src="'+I+'" alt="趋势递增"></p><p>趋势递增：T<sub>n</sub>&gt;T<sub>n-s</sub>，那么大概率有F(T<sub>n</sub>)&gt;F(T<sub>n-s</sub>)。虽然在一段时间间隔内有乱序，但是整体趋势是递增。从上图上看，是有上升趋势的（趋势线）。</p><ul><li>在<strong>SnowflakeId</strong>中<sub>n-s</sub>受到全局时钟同步影响。</li><li>在号段模式(<strong>SegmentId</strong>)中<sub>n-s</sub>受到号段可用区间(<code>Step</code>)影响。</li></ul><h2 id="分布式id分配方案" tabindex="-1">分布式ID分配方案 <a class="header-anchor" href="#分布式id分配方案" aria-label="Permalink to &quot;分布式ID分配方案&quot;">​</a></h2><h3 id="uuid-guid" tabindex="-1">UUID/GUID <a class="header-anchor" href="#uuid-guid" aria-label="Permalink to &quot;UUID/GUID&quot;">​</a></h3><ul><li>👍不依赖任何第三方中间件</li><li>👍性能高</li><li>👎完全无序</li><li>👎空间占用大，需要占用128位存储空间。</li></ul><p>UUID最大的缺陷是随机的、无序的，当用于主键时会导致数据库的主键索引效率低下（为了维护索引树，频繁的索引中间位置插入数据，而不是追加写）。这也是UUID不适用于数据库主键的最为重要的原因。</p><h3 id="snowflakeid" tabindex="-1">SnowflakeId <a class="header-anchor" href="#snowflakeid" aria-label="Permalink to &quot;SnowflakeId&quot;">​</a></h3><p><em>SnowflakeId</em> 使用<code>Long</code>（64-bit）位分区来生成ID的一种分布式ID算法。 通用的位分配方案为：<code>timestamp</code>(41-bit)+<code>machineId</code>(10-bit)+<code>sequence</code>(12-bit)=63-bit。</p><p align="center"><img src="'+o+'" alt="SnowflakeId"></p><ul><li>41-bit<code>timestamp</code>=(1L&lt;&lt;41)/(1000/3600/24/365)，约可以存储69年的时间戳，即可以使用的绝对时间为<code>EPOCH</code>+69年，一般我们需要自定义<code>EPOCH</code>为产品开发时间，另外还可以通过压缩其他区域的分配位数，来增加时间戳位数来延长可用时间。</li><li>10-bit<code>machineId</code>=(1L&lt;&lt;10)=1024，即相同业务可以部署1024个副本(在Kubernetes概念里没有主从副本之分，这里直接沿用Kubernetes的定义)。一般情况下没有必要使用这么多位，所以会根据部署规模需要重新定义。</li><li>12-bit<code>sequence</code>=(1L&lt;&lt;12)*1000=4096000，即单机每秒可生成约409W的ID，全局同业务集群可产生<code>4096000*1024=419430W=41.9亿(TPS)</code>。</li></ul><p>从 <em>SnowflakeId</em> 设计上可以看出:</p><ul><li>👍 <code>timestamp</code>在高位，单实例<em>SnowflakeId</em>是会保证时钟总是向前的（校验本机时钟回拨），所以是本机单调递增的。受全局时钟同步/时钟回拨影响<em>SnowflakeId</em>是全局趋势递增的。</li><li>👍 <em>SnowflakeId</em>不对任何第三方中间件有强依赖关系，并且性能也非常高。</li><li>👍 位分配方案可以按照业务系统需要灵活配置，来达到最优使用效果。</li><li>👎 强依赖本机时钟，潜在的时钟回拨问题会导致ID重复、处于短暂的不可用状态。</li><li>👎 <code>machineId</code>需要手动设置，实际部署时如果采用手动分配<code>machineId</code>，会非常低效。</li></ul><h4 id="snowflakeid之机器号分配问题" tabindex="-1">SnowflakeId之机器号分配问题 <a class="header-anchor" href="#snowflakeid之机器号分配问题" aria-label="Permalink to &quot;SnowflakeId之机器号分配问题&quot;">​</a></h4><p>在<strong>SnowflakeId</strong>中根据业务设计的位分配方案确定了基本上就不再有变更了，也很少需要维护。但是<code>machineId</code>总是需要配置的，而且集群中是不能重复的，否则分区原则就会被破坏而导致ID唯一性原则破坏，当集群规模较大时<code>machineId</code>的维护工作是非常繁琐，低效的。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>有一点需要特别说明的，<strong>SnowflakeId</strong> 的 <strong>MachineId</strong> 是逻辑上的概念，而不是物理概念，所以称之为 <code>WorkerId</code> 更为准确。</p><p>想象一下假设 <strong>MachineId</strong> 是物理上的，那么意味着一台机器拥有只能拥有一个 <strong>MachineId</strong>，那会产生什么问题呢？</p></div><p>目前 <em><a href="https://github.com/Ahoo-Wang/CosId" target="_blank" rel="noreferrer">CosId</a></em> 提供了以下五种 <code>MachineId</code> 分配器。</p><ul><li>ManualMachineIdDistributor: 手动配置<code>machineId</code>，一般只有在集群规模非常小的时候才有可能使用，不推荐。</li><li>StatefulSetMachineIdDistributor: 使用<code>Kubernetes</code>的<code>StatefulSet</code>提供的稳定的标识ID（HOSTNAME=service-01）作为机器号。</li><li>RedisMachineIdDistributor: 使用<strong>Redis</strong>作为机器号的分发存储，同时还会存储<code>MachineId</code>的上一次时间戳，用于<strong>启动时时钟回拨</strong>的检查。</li><li>JdbcMachineIdDistributor: 使用<strong>关系型数据库</strong>作为机器号的分发存储，同时还会存储<code>MachineId</code>的上一次时间戳，用于<strong>启动时时钟回拨</strong>的检查。</li><li>ZookeeperMachineIdDistributor: 使用<strong>ZooKeeper</strong>作为机器号的分发存储，同时还会存储<code>MachineId</code>的上一次时间戳，用于<strong>启动时时钟回拨</strong>的检查。</li></ul><p align="center"><img src="'+d+'" alt="MachineIdDistributor"></p><p align="center"><img src="'+n+'" alt="Machine Id Safe Guard"></p><h4 id="snowflakeid之时钟回拨问题" tabindex="-1">SnowflakeId之时钟回拨问题 <a class="header-anchor" href="#snowflakeid之时钟回拨问题" aria-label="Permalink to &quot;SnowflakeId之时钟回拨问题&quot;">​</a></h4><p>时钟回拨的致命问题是会导致ID重复、冲突（这一点不难理解），ID重复显然是不能被容忍的。 在<strong>SnowflakeId</strong>算法中，按照<strong>MachineId</strong>分区ID，我们不难理解的是不同<strong>MachineId</strong>是不可能产生相同ID的。所以我们解决的时钟回拨问题是指当前<strong>MachineId</strong>的时钟回拨问题，而不是所有集群节点的时钟回拨问题。</p><p><strong>MachineId</strong>时钟回拨问题大体可以分为俩种情况：</p><ul><li>运行时时钟回拨：即在运行时获取的当前时间戳比上一次获取的时间戳小。这个场景的时钟回拨是很容易处理的，一般<strong>SnowflakeId</strong>代码实现时都会存储<code>lastTimestamp</code>用于运行时时钟回拨的检查，并抛出时钟回拨异常。 <ul><li>时钟回拨时直接抛出异常是不太好地实践，因为下游使用方几乎没有其他处理方案（噢，我还能怎么办呢，等吧），时钟同步是唯一的选择，当只有一种选择时就不要再让用户选择了。</li><li><code>ClockSyncSnowflakeId</code>是<code>SnowflakeId</code>的包装器，当发生时钟回拨时会使用<code>ClockBackwardsSynchronizer</code>主动等待时钟同步来重新生成ID，提供更加友好的使用体验。</li></ul></li><li>启动时时钟回拨：即在启动服务实例时获取的当前时钟比上次关闭服务时小。此时的<code>lastTimestamp</code>是无法存储在进程内存中的。当获取的外部存储的<strong>机器状态</strong>大于当前时钟时钟时，会使用<code>ClockBackwardsSynchronizer</code>主动同步时钟。 <ul><li>LocalMachineStateStorage：使用本地文件存储<code>MachineState</code>(机器号、最近一次时间戳)。因为使用的是本地文件所以只有当实例的部署环境是稳定的，<code>LocalMachineStateStorage</code>才适用。</li><li>RedisMachineIdDistributor：将<code>MachineState</code>存储在<strong>Redis</strong>分布式缓存中，这样可以保证总是可以获取到上次服务实例停机时<strong>机器状态</strong>。</li></ul></li></ul><h4 id="snowflakeid之javascript数值溢出问题" tabindex="-1">SnowflakeId之JavaScript数值溢出问题 <a class="header-anchor" href="#snowflakeid之javascript数值溢出问题" aria-label="Permalink to &quot;SnowflakeId之JavaScript数值溢出问题&quot;">​</a></h4><p><code>JavaScript</code>的<code>Number.MAX_SAFE_INTEGER</code>只有53-bit，如果直接将63位的<code>SnowflakeId</code>返回给前端，那么会产生值溢出的情况（所以这里我们应该知道后端传给前端的<code>long</code>值溢出问题，<strong>迟早</strong>会出现，只不过SnowflakeId出现得更快而已）。 很显然溢出是不能被接受的，一般可以使用以下俩种处理方案：</p><ul><li>将生成的63-bit<code>SnowflakeId</code>转换为<code>String</code>类型。 <ul><li>直接将<code>long</code>转换成<code>String</code>。</li><li>使用<code>SnowflakeFriendlyId</code>将<code>SnowflakeId</code>转换成比较友好的字符串表示：<code>{timestamp}-{machineId}-{sequence} -&gt; 20210623131730192-1-0</code></li></ul></li><li>自定义<code>SnowflakeId</code>位分配来缩短<code>SnowflakeId</code>的位数（53-bit）使 <code>ID</code> 提供给前端时不溢出 <ul><li>使用<code>SafeJavaScriptSnowflakeId</code>(<code>JavaScript</code> 安全的 <code>SnowflakeId</code>)</li></ul></li></ul><h3 id="号段模式-segmentid" tabindex="-1">号段模式（SegmentId） <a class="header-anchor" href="#号段模式-segmentid" aria-label="Permalink to &quot;号段模式（SegmentId）&quot;">​</a></h3><p align="center"><img src="'+l+'" alt="SegmentId"></p><p>从上面的设计图中，不难看出<strong>号段模式</strong>基本设计思路是通过每次获取一定长度（Step）的可用ID（Id段/号段），来降低网络IO请求次数，提升性能。</p><ul><li>👎强依赖第三方号段分发器，可用性受到第三方分发器影响。</li><li>👎每次号段用完时获取<code>NextMaxId</code>需要进行网络IO请求，此时的性能会比较低。</li><li>单实例ID单调递增，全局趋势递增。 <ul><li>从设计图中不难看出<strong>Instance 1</strong>每次获取的<code>NextMaxId</code>，一定比上一次大，意味着下一次的号段一定比上一次大，所以从单实例上来看是单调递增的。</li><li>多实例各自持有的不同的号段，意味着同一时刻不同实例生成的ID是乱序的，但是整体趋势的递增的，所以全局趋势递增。</li></ul></li><li>ID乱序程度受到Step长度以及集群规模影响（从趋势递增图中不难看出）。 <ul><li>假设集群中只有一个实例时<strong>号段模式</strong>就是单调递增的。</li><li><code>Step</code>越小，乱序程度越小。当<code>Step=1</code>时，将无限接近单调递增。需要注意的是这里是无限接近而非等于单调递增，具体原因你可以思考一下这样一个场景： <ul><li>号段分发器T<sub>1</sub>时刻给<strong>Instance 1</strong>分发了<code>ID=1</code>,T<sub>2</sub>时刻给<strong>Instance 2</strong>分发了<code>ID=2</code>。因为机器性能、网络等原因，<code>Instance 2</code>网络IO写请求先于<code>Instance 1</code>到达。那么这个时候对于数据库来说，ID依然是乱序的。</li></ul></li></ul></li></ul><h3 id="号段链模式-segmentchainid" tabindex="-1">号段链模式（SegmentChainId） <a class="header-anchor" href="#号段链模式-segmentchainid" aria-label="Permalink to &quot;号段链模式（SegmentChainId）&quot;">​</a></h3><p align="center"><img src="'+i+'" alt="SegmentChainId"></p><p><strong>SegmentChainId</strong>是<strong>SegmentId</strong>增强版，相比于<strong>SegmentId</strong>有以下优势：</p><ul><li>稳定性：<strong>SegmentId</strong>的稳定性问题（P9999=46.624(us/op)）主要是因为号段用完之后同步进行<code>NextMaxId</code>的获取导致的（会产生网络IO）。 <ul><li><strong>SegmentChainId</strong> （P9999=0.208(us/op)）引入了新的角色<strong>PrefetchWorker</strong>用以维护和保证<strong>安全距离</strong>，理想情况下使得获取ID的线程几乎完全不需要进行同步的等待<code>NextMaxId</code>获取，性能可达到近似 <code>AtomicLong</code> 的 <em>TPS 性能:12743W+/s</em> <a href="./faq/perf-test.html">JMH 基准测试</a> 。</li></ul></li><li>适应性：从<strong>SegmentId</strong>介绍中我们知道了影响<strong>ID乱序</strong>的因素有俩个：集群规模、<code>Step</code>大小。集群规模是我们不能控制的，但是<code>Step</code>是可以调节的。 <ul><li><code>Step</code>应该近可能小才能使得<strong>ID单调递增</strong>的可能性增大。</li><li><code>Step</code>太小会影响吞吐量，那么我们如何合理设置<code>Step</code>呢？答案是我们无法准确预估所有时点的吞吐量需求，那么最好的办法是吞吐量需求高时，Step自动增大，吞吐量低时Step自动收缩。</li><li><strong>SegmentChainId</strong>引入了<strong>饥饿状态</strong>的概念，<strong>PrefetchWorker</strong>会根据<strong>饥饿状态</strong>检测当前<strong>安全距离</strong>是否需要膨胀或者收缩，以便获得吞吐量与有序性之间的权衡，这便是<strong>SegmentChainId</strong>的自适应性。</li></ul></li></ul>',58)]))}const M=a(h,[["render",m]]);export{w as __pageData,M as default};
